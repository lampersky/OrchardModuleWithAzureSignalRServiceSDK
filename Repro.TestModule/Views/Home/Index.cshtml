<pre>
Hello from Repro.TestModule
1) Open dev console in your browser,
2) Go to Network tab,
3) Filter to WebSockets,
4) Select first request,
5) Open Messages tab,
6) Find message with data {"type":6}
</pre>

<script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/5.0.2/signalr.min.js"></script>
<script>
    const connection = new signalR.HubConnectionBuilder().withUrl("/testHub").build();
    connection.start().then(() => {
        console.log('started');
    }).catch((error) => {
        return console.error(error.toString());
    });
</script>